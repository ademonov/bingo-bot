/// Returns the words from the string
pub fn split<'a, I>(s: &'a str) -> I where I: Iterator<Item = &'a &'a str> {
    unimplemented!()
}

pub struct Word {
    title: String,
    words: Vec<String>
}

impl Word {
     fn new() -> Word {
         unimplemented!()
     }
}